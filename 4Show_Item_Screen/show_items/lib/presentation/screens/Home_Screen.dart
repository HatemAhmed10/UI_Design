import 'package:flutter/material.dart';

class Home_Screnn extends StatelessWidget {
  const Home_Screnn({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Home"),
      ),
    );
  }
}
